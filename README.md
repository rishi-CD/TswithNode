this is an a backend code ised for GET and POST methods

# Table of Content

     Installation
     API points

# Installation
     
    # clone repository
    # if using http
    git clone https://github.com/rishi-CD/TswithNode.git
    # if using ssh
    git clone git@github.com:rishi-CD/TswithNode.git

## Package

"nodemon": "3.1.10"
"express": "5.1.0"
"npm" : "10.9.0"

# End Point

    created an api end point used for 
    GET endpoint to fetch all users and 
    POST endpoint to create a new user.